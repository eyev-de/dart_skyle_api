name: skyle_api
description: Dart implementation of the Skyle API.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  collection: ^1.16.0

  freezed_annotation: ^2.1.0

  grpc: ^3.0.2

  http: ^0.13.5

  json_annotation: ^4.6.0

  logger: ^1.1.0

  path: ^1.8.0
  
  protobuf: ^2.0.0

  retry: ^3.1.0

  universal_platform: ^1.0.0+1

dev_dependencies:
  build_runner: ^2.2.0
  freezed: ^2.1.0+1
  json_serializable: ^6.3.1
  test: ^1.20.2
